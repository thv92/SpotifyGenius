import React from 'react';
import SearchPage from '../SearchPage/SearchPage';
import styles from './App.css';

export default () => {
    return (
        <main>
            <SearchPage />
        </main>
    );
};